{
  "private": true,
  "name": "@marcus-rise/react-theme",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @marcus-rise/react-theme dev",
    "build": "yarn workspace @marcus-rise/react-theme build && yarn workspace docs build"
  }
}
